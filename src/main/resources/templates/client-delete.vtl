<form action="/stylists/$stylist.getId()/clients/$client.getId()/delete" method="post">
  <p>Are you sure you want to delete $client.getName()?</p>
  <button type="submit" class="btn btn-default delete">Delete</button>
</form>
